import iconWallet from './images/icon-wallet.svg';

const HeaderIcon = ({ className }) => {
  return (
    <img className={className} src={iconWallet} alt="icon wallet" />
  )
}

export default HeaderIcon;