import iconLogout from './images/icon-logout.svg';

const LogoutIcon = ({ className }) => {
  return (
    <img className={className} src={iconLogout} alt="logout icon" />
  )
}

export default LogoutIcon;